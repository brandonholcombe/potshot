# UI — Menus + HUD dial-in (after M2, Brandon-requested)

## Author: (assign on activation)
## Status: Backlog — scheduled after M2 netcode

Brandon (2026-08-04): "we need to go over menus, and we will dial in the
HUD." Scope to define with him at activation; expected shape:

- **Main menu**: title, Play (host/join at M2+ — server address field or
  server browser-lite), map select, quit. Replaces the current
  boot-into-DevArena flow.
- **In-game HUD**: replace DevHud IMGUI with real UI (uGUI, generated by
  editor scripts per zero-GUI rule): health bar, weapon + ammo, boost
  state, scoreboard (hold Tab?), round timer, kill feed, death/respawn
  countdown, round-over standings.
- **Pause/options menu**: resume, volume (when audio exists), quit to
  menu, disconnect.
- Visual pass: consistent with the eventual art direction (Kenney UI pack
  is the CC0 default candidate — docs/assets.md rules apply).
- Keep IMGUI DevHud available behind a dev flag for agent screenshots.

Design questions to ask Brandon at kickoff: controller support?
resolution/windowed options? name entry for multiplayer identity (M2
ties into this)?
